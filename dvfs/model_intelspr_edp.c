/* Auto-generated by Python ML Exporter */#include "model.h"
#include <stdlib.h>

FreqLevel predict_phase_level(double CPI, double Math_Intensity, double Stall_Ratio, double System_BW_Proxy, double Branch_MPKI, double GFLOPS_Approx, double Clock_Ratio) {
    (void)GFLOPS_Approx;
    if (Stall_Ratio <= 0.048326) {
        if (Clock_Ratio <= 0.319261) {
            // Prediction: MedFreq
            return FREQ_LEVEL_MED;
        } else {
            if (Stall_Ratio <= 0.040047) {
                if (Branch_MPKI <= 0.000268) {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                } else {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                }
            } else {
                if (CPI <= 0.464993) {
                    // Prediction: MedFreq
                    return FREQ_LEVEL_MED;
                } else {
                    if (CPI <= 0.551856) {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    } else {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    }
                }
            }
        }
    } else {
        if (Math_Intensity <= 0.000005) {
            if (System_BW_Proxy <= 0.000833) {
                if (Clock_Ratio <= 0.652800) {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                } else {
                    // Prediction: MedFreq
                    return FREQ_LEVEL_MED;
                }
            } else {
                // Prediction: HighFreq
                return FREQ_LEVEL_HIGH;
            }
        } else {
            if (Math_Intensity <= 0.821852) {
                if (Branch_MPKI <= 7.849046) {
                    if (System_BW_Proxy <= 0.000675) {
                        // Prediction: MedFreq
                        return FREQ_LEVEL_MED;
                    } else {
                        // Prediction: MedFreq
                        return FREQ_LEVEL_MED;
                    }
                } else {
                    if (CPI <= 4.167470) {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    } else {
                        // Prediction: MedFreq
                        return FREQ_LEVEL_MED;
                    }
                }
            } else {
                if (Branch_MPKI <= 0.072809) {
                    // Prediction: MedFreq
                    return FREQ_LEVEL_MED;
                } else {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                }
            }
        }
    }
}
