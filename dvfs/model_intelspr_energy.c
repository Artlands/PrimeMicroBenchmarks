/* Auto-generated by Python ML Exporter */#include "model.h"
#include <stdlib.h>

FreqLevel predict_phase_level(double CPI, double Math_Intensity, double Stall_Ratio, double System_BW_Proxy, double Branch_MPKI, double GFLOPS_Approx, double Clock_Ratio) {
    (void)GFLOPS_Approx;
    if (Branch_MPKI <= 1.566219) {
        if (System_BW_Proxy <= 5.543217) {
            if (CPI <= 0.216268) {
                // Prediction: LowFreq
                return FREQ_LEVEL_LOW;
            } else {
                // Prediction: HighFreq
                return FREQ_LEVEL_HIGH;
            }
        } else {
            if (Branch_MPKI <= 0.000033) {
                // Prediction: LowFreq
                return FREQ_LEVEL_LOW;
            } else {
                // Prediction: LowFreq
                return FREQ_LEVEL_LOW;
            }
        }
    } else {
        if (Stall_Ratio <= 0.032005) {
            if (CPI <= 0.270619) {
                // Prediction: LowFreq
                return FREQ_LEVEL_LOW;
            } else {
                // Prediction: HighFreq
                return FREQ_LEVEL_HIGH;
            }
        } else {
            if (System_BW_Proxy <= 0.127561) {
                if (Clock_Ratio <= 0.319109) {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                } else {
                    if (Branch_MPKI <= 2.220281) {
                        // Prediction: MedFreq
                        return FREQ_LEVEL_MED;
                    } else {
                        // Prediction: MedFreq
                        return FREQ_LEVEL_MED;
                    }
                }
            } else {
                if (Math_Intensity <= 0.000492) {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                } else {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                }
            }
        }
    }
}
