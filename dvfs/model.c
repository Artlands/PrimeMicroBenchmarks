/* Auto-generated by Python ML Exporter */
#include "model.h"
#include <stdlib.h>

FreqLevel predict_phase_level(double CPI, double Math_Intensity, double Stall_Ratio, double System_BW_Proxy, double Branch_MPKI, double GFLOPS_Approx, double Clock_Ratio) {
    (void)Branch_MPKI;
    (void)GFLOPS_Approx;
    if (Math_Intensity <= 0.076391) {
        if (CPI <= 0.877714) {
            if (System_BW_Proxy <= 0.000616) {
                if (CPI <= 0.354711) {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                } else {
                    // Prediction: LowFreq
                    return FREQ_LEVEL_LOW;
                }
            } else {
                if (System_BW_Proxy <= 0.000675) {
                    if (CPI <= 0.386396) {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    } else {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    }
                } else {
                    if (System_BW_Proxy <= 0.001014) {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    } else {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    }
                }
            }
        } else {
            if (Stall_Ratio <= 0.522978) {
                if (Clock_Ratio <= 0.998041) {
                    if (System_BW_Proxy <= 1.036247) {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    } else {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    }
                } else {
                    if (CPI <= 64.026469) {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    } else {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    }
                }
            } else {
                // Prediction: LowFreq
                return FREQ_LEVEL_LOW;
            }
        }
    } else {
        if (Stall_Ratio <= 0.052943) {
            // Prediction: HighFreq
            return FREQ_LEVEL_HIGH;
        } else {
            if (Stall_Ratio <= 0.087929) {
                // Prediction: MedFreq
                return FREQ_LEVEL_MED;
            } else {
                // Prediction: MedFreq
                return FREQ_LEVEL_MED;
            }
        }
    }
}
