/* Auto-generated by Python ML Exporter */
#include "model.h"
#include <stdlib.h>

FreqLevel predict_phase_level(double CPI, double Compute_Density, double Mem_Boundness, double Stall_Ratio, double Branch_MPKI, double Vector_Intensity) {
    (void)CPI;
    (void)Vector_Intensity;
    if (Compute_Density <= 5.402331) {
        if (Branch_MPKI <= 0.135086) {
            if (Stall_Ratio <= 0.022998) {
                // Prediction: HighFreq
                return FREQ_LEVEL_HIGH;
            } else {
                if (Compute_Density <= 0.740776) {
                    if (Mem_Boundness <= 0.000001) {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    } else {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    }
                } else {
                    if (Compute_Density <= 1.193717) {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    } else {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    }
                }
            }
        } else {
            if (Stall_Ratio <= 0.638048) {
                if (Compute_Density <= 0.036957) {
                    if (Mem_Boundness <= 0.134733) {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    } else {
                        // Prediction: HighFreq
                        return FREQ_LEVEL_HIGH;
                    }
                } else {
                    // Prediction: LowFreq
                    return FREQ_LEVEL_LOW;
                }
            } else {
                if (Mem_Boundness <= 0.171512) {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                } else {
                    if (Branch_MPKI <= 0.136054) {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    } else {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    }
                }
            }
        }
    } else {
        // Prediction: MedFreq
        return FREQ_LEVEL_MED;
    }
}
