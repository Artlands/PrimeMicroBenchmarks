/* Auto-generated by Python ML Exporter */#include "model.h"
#include <stdlib.h>

FreqLevel predict_phase_level(double CPI, double Math_Intensity, double Stall_Ratio, double System_BW_Proxy, double Branch_MPKI, double GFLOPS_Approx, double Clock_Ratio) {
    (void)CPI;
    (void)GFLOPS_Approx;
    (void)Clock_Ratio;
    if (Stall_Ratio <= 0.142633) {
        if (Branch_MPKI <= 0.058580) {
            if (Math_Intensity <= 1.119242) {
                if (System_BW_Proxy <= 0.001047) {
                    // Prediction: HighFreq
                    return FREQ_LEVEL_HIGH;
                } else {
                    if (Branch_MPKI <= 0.055415) {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    } else {
                        // Prediction: LowFreq
                        return FREQ_LEVEL_LOW;
                    }
                }
            } else {
                // Prediction: HighFreq
                return FREQ_LEVEL_HIGH;
            }
        } else {
            if (System_BW_Proxy <= 0.696012) {
                // Prediction: HighFreq
                return FREQ_LEVEL_HIGH;
            } else {
                if (System_BW_Proxy <= 0.974809) {
                    // Prediction: LowFreq
                    return FREQ_LEVEL_LOW;
                } else {
                    // Prediction: MedFreq
                    return FREQ_LEVEL_MED;
                }
            }
        }
    } else {
        if (Branch_MPKI <= 41.835681) {
            // Prediction: MedFreq
            return FREQ_LEVEL_MED;
        } else {
            // Prediction: HighFreq
            return FREQ_LEVEL_HIGH;
        }
    }
}
